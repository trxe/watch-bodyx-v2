import themes from '../styles/Themes.module.css'

export const showName = 'BODYX';
export const themeName = themes.bodyx;

export const LOCATIONS = {
    POLL: '#poll',
    DISCONNECTED: '#disconnected',
    HOME: '#',
}